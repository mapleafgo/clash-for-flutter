/// 常量类
class Constant {
  /// 下载的配置文件路径
  static const profilesPath = "./profiles";

  /// 配置文件夹
  static const configDir = "./.config";

  /// clash的配置
  static const clashConfig = "./.config/config.json";

  /// 该程序的一些配置
  static const clashForMe = "./.config/cfm.json";
}
